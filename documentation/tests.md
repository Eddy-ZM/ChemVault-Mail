# Verification

Pull requests install the Vue and Worker lockfiles, build the client, and run Worker tests with a 30-second hook timeout. Required coverage includes permission mapping, external Access defaults, send limits, missing-KV fail-closed behavior, initialization-secret separation, provider adapters, and lifecycle deletion.
