# Permissions

Viewing or deleting all mail requires explicit `all-email:query` or `all-email:delete` permission. External Cloudflare Access identities default to no permissions and must be assigned an approved external role. The administrator mailbox bypass is limited to the configured administrator identity.
